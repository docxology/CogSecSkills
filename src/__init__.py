"""Source root for CogSecSkills.

The project's code lives in the :mod:`cogsecskills` package alongside this file.
This module is intentionally minimal so ``src`` stays a clean namespace on the
import path; import the runner via ``import cogsecskills``.
"""
